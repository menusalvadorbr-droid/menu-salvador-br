import { NextResponse } from 'next/server'

// TODO: implementar esta rota. Handler stub para evitar quebrar o build
// (o arquivo estava vazio, o que não é um módulo de rota válido no Next.js).
export async function POST() {
  return NextResponse.json({ error: 'Não implementado' }, { status: 501 })
}
